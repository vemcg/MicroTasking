<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Cross-app messages with ActiveTasks (see TaskEvents.kt / SPEC.md "Synchronization"). Both
         apps declare the same signature-level permission (so install order doesn't matter) and
         only an app signed with the same key can hold it - hence the shared debug keystore. -->
    <permission
        android:name="com.vernmcgeorge.tasks.permission.TASK_EVENTS"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.vernmcgeorge.tasks.permission.TASK_EVENTS" />

    <queries>
        <package android:name="com.activetasks.app" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".PromptAlarmReceiver"
            android:exported="false" />

        <!-- Exported so ActiveTasks can reach it, but only with the signature permission above. -->
        <receiver
            android:name=".TaskEventReceiver"
            android:exported="true"
            android:permission="com.vernmcgeorge.tasks.permission.TASK_EVENTS" />

    </application>

</manifest>
